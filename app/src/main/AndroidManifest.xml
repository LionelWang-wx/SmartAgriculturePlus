<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myapp.smartagricultureplus">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- 获取手机相机的权限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <uses-permission
        android:name="android.permission.ACCESS_WIFI_STATE" />
    允许访问WiFi网络状态信息（必须）
    <uses-permission
        android:name="android.permission.READ_PHONE_STATE" />
    允许读取手机状态（必须）
    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE" />
    允许访问网络状态（必须）
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    允许写手机存储（必须）
    <uses-permission
        android:name="android.permission.RECEIVE_SMS" />
    GUI自动填充验证码功能（非必须）
    <uses-permission
        android:name="android.permission.READ_SMS" />
    从短信中读取验证码接口（非必须）
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartAgriculturePlus">
        <activity android:name=".Activity.TextActivity"></activity>
        <activity android:name=".Activity.Scanning_DeviceActivity" />
        <activity android:name=".Activity.OtherActivity" />
        <activity android:name=".Activity.MainActivity" />
        <activity android:name=".Activity.BaseActivity" />
        <activity android:name=".Activity.LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.NavigationActivity" />
    </application>

</manifest>